function Porcentagem(num1, num2) 
{
    return (num1 / 100) * num2; // Calcula a porcentagem de num1 em relação a num2.
    
}

module.exports = Porcentagem; // Exporta a função para ser usada em outros arquivos.
